info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: "{{link}}/{{api_auth_link}}/register"
  body:
    type: json
    data: |-
      {
        "name": "testing4",
        "email": "testing4@gmail.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
