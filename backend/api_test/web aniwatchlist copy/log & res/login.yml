info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{link}}/{{api_auth_link}}/login"
  body:
    type: json
    data: |-
      {
        "email": "testing4@gmail.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
